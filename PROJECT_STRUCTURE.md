# هيكل المشروع - Project Structure

## 📁 الهيكل المنظم للمشروع

```
src/
├── app/                    # صفحات Next.js
│   ├── layout.jsx          # Layout الرئيسي
│   ├── page.jsx            # الصفحة الرئيسية (منظمة الآن)
│   └── globals.css         # الأنماط العامة
│
├── components/             # المكونات
│   ├── I18nProvider.jsx    # موفر الترجمة
│   │
│   ├── layout/             # مكونات التخطيط
│   │   ├── Header.jsx      # رأس الصفحة
│   │   ├── Footer.jsx      # تذييل الصفحة
│   │   ├── LanguageSwitcher.jsx  # مبدل اللغة
│   │   └── index.js        # ملف التصدير
│   │
│   └── sections/           # أقسام الصفحة
│       ├── Hero.jsx        # قسم البطل
│       ├── About.jsx       # قسم من نحن
│       ├── ChangeLandUse.jsx  # قسم تغيير استعمال الأراضي
│       ├── Services.jsx    # قسم الخدمات
│       ├── Contact.jsx     # قسم الاتصال
│       ├── SuccessStories.jsx  # قصص النجاح
│       ├── Testimonials.jsx    # آراء العملاء
│       ├── CTA.jsx         # دعوة للعمل
│       └── index.js        # ملف التصدير
│
├── hooks/                  # Custom Hooks
│   └── useLanguage.js      # Hook لإدارة اللغة والاتجاه
│
├── i18n.js                 # إعدادات الترجمة
│
└── locales/                # ملفات الترجمة
    ├── ar.json             # الترجمة العربية
    └── en.json             # الترجمة الإنجليزية
```

## ✨ المميزات

### 1. **التنظيم**
- كل قسم في ملف منفصل
- مكونات Layout منفصلة
- Hooks مخصصة لإدارة الحالة

### 2. **سهولة الصيانة**
- كود منظم وسهل القراءة
- كل مكون له مسؤولية واحدة
- سهولة إضافة أو تعديل الأقسام

### 3. **إعادة الاستخدام**
- المكونات قابلة لإعادة الاستخدام
- Hooks قابلة للاستخدام في أي مكان

### 4. **الدعم متعدد اللغات**
- دعم كامل للعربية والإنجليزية
- تغيير تلقائي للاتجاه (RTL/LTR)
- حفظ تفضيلات اللغة

## 🚀 الاستخدام

### استيراد المكونات:
```jsx
// من sections
import { Hero, About, Services } from '@/components/sections';

// من layout
import { Header, Footer } from '@/components/layout';

// استخدام Hook
import { useLanguage } from '@/hooks/useLanguage';
```

### إضافة قسم جديد:
1. أنشئ ملف جديد في `src/components/sections/`
2. أضف الترجمة في `src/locales/ar.json` و `en.json`
3. استورد المكون في `src/app/page.jsx`

## 📝 ملاحظات

- جميع المكونات تستخدم `'use client'` لأنها تحتاج إلى تفاعل
- الترجمة تتم عبر `react-i18next`
- الاتجاه (RTL/LTR) يتم إدارته تلقائياً عبر `useLanguage` hook

